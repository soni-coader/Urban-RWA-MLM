 
export function loadSplineScene(url) {
    console.log(`  loading Spline scene from: ${url}`);
    return url;  
}

export function logModelConfig(config) {
    console.log("Model config:", config);
}
